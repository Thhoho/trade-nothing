# The deterministic core, installation, validation, and host adapters use only
# the Python standard library. No third-party package is required to install or
# run the release gates.

# Optional experimental macro observations used by scripts/verified_fetcher.py:
# yfinance>=0.2.40
# akshare>=1.14.0

# Install optional providers only after explicit user approval. Provider output
# remains contextual until it passes the normal citation and claim gates.
